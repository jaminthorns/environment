{
  "Log to the console (labeled)": {
    "prefix": "log",
    "body": ["console.log('$1', $2)"]
  },
  "Time a section of code": {
    "prefix": "time",
    "body": ["console.time('$1')", "console.timeEnd('$1')"]
  },
  "Create a React component": {
    "prefix": "component",
    "body": ["export ${1:default} function $2({ $3 }) {", "  return $4", "}"]
  }
}
