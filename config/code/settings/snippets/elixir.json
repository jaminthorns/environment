{
  "put": {
    "prefix": "put",
    "body": ["IO.puts(\"$1\")"],
    "description": "Print a string"
  },
  "inspect": {
    "prefix": "ins",
    "body": ["IO.inspect($1, label: \"$2\")"],
    "description": "Inspect a value"
  },
  "inspect_piped": {
    "prefix": "ins",
    "body": ["IO.inspect(label: \"$1\")"],
    "description": "Inspect a value (piped)"
  },
  "pry": {
    "prefix": "pry",
    "body": ["require IEx\nIEx.pry()"],
    "description": "Debug some code"
  }
}
